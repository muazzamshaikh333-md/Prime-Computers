# MongoDB connection string
MONGODB_URI=mongodb://127.0.0.1:27017/prime_computers

# Gmail you will send FROM (the cyber cafe's notification email)
EMAIL_USER=muazzamshaikh333@gmail.com

# Gmail App Password (NOT your normal Gmail password — see README for how to get this)
EMAIL_PASS=9665784689@md

# Email that should RECEIVE the print request notifications (owner's email)
OWNER_EMAIL=gufrantirandaz@gmail.com

# Secret key to protect the admin dashboard. Make up any long random string.
# Whoever knows this key can view/manage all print requests, so keep it private.
ADMIN_KEY=Muazzam@123

# Port the backend server runs on
PORT=5000
